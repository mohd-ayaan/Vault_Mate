<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="/doc.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <title>VAULT MATE</title>
</head>
<body>
    <header class="header">
        <div class="container">
            <a class="logo" href="/"> 
                <img class="logo-image" src="\2.png" alt="Vault Mate Logo">
                <span class="site-title">VAULT MATE</span>
            </a>
            <nav class="menu">
                <ul>
                    <li><a href="/">Home</a></li>
                    <li><a href="/about">About</a></li>
                    <li><a href="/contact">Contact</a></li>
                    <li><a href="/logout">LogOut</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main class="doc-main-content">
        <h1 class="doc-heading"><%= filename %></h1>

        <div class="upload-card">
            <div class="image-preview">
                <% if (!mimeType || mimeType === 'unknown') { %>
                  <p style="text-align:center;">No document uploaded yet.</p>
                <% } else if (mimeType === 'application/pdf') { %>
                  <iframe src="/image/<%= filename %>" width="100%" height="600px" style="border:none;"></iframe>
                <% } else { %>
                  <img src="/image/<%= filename %>" alt="Document Preview" id="document-display">
                <% } %>

            </div>

            <form action='/upload/<%=filename%>' method="post" enctype="multipart/form-data" class="upload-form">
                <div class="file-input-group">
                    <label for="image" class="file-input-label">Choose document:</label>
                    <input type="file" name="image" id="image" accept=".jpg, .jpeg, .png, .pdf" required>
                    <span id="file-name" class="selected-file-name">No file chosen</span>
                </div>
                <div class="form-actions">
                    <button type="submit" class="button upload-button">Upload</button>
                    <a href="/image/<%=filename%>" target="_blank" class="button preview-button">Preview</a>
                </div>
            </form>
        </div>
    </main>

    <footer class="footer">
        <h3>&copy; <%= new Date().getFullYear() %> Vault Mate. All rights reserved. | <a href="/privacy" style="color: inherit; text-decoration: none;">Privacy Policy</a> | <a href="/terms" style="color: inherit; text-decoration: none;">Terms of Service</a></h3>
    </footer>

    <script>
        // JavaScript to display the selected file name
        document.getElementById('image').addEventListener('change', function() {
            const fileNameSpan = document.getElementById('file-name');
            if (this.files && this.files.length > 0) {
                fileNameSpan.textContent = this.files[0].name;
            } else {
                fileNameSpan.textContent = 'No file chosen';
            }
        });
    </script>
</body>
</html>