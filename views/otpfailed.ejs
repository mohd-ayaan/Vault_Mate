<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OTP Failed - Vault Mate</title>
    <style>
        /* Core styles for the Vault Mate application consistency */

        body {
            font-family: 'Arial', sans-serif;
            background-color: #F8E7E7;
            /* Light pink background */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
            box-sizing: border-box;
        }

        /* Card Container Style */
        .form-container {
            background-color: #ffffff;
            /* White card background */
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            width: 100%;
            text-align: center;
            box-sizing: border-box;
        }

        /* Headings */
        h2 {
            color: #C0516E;
            /* Pink accent color for main headings */
            margin-bottom: 25px;
            font-size: 1.8em;
            font-weight: bold;
        }

        /* Paragraph Text (for OTP explanation) */
        p {
            color: #555;
            margin-bottom: 20px;
            line-height: 1.5;
        }

        /* Form Layout */
        form {
            display: flex;
            flex-direction: column;
            gap: 15px;
            /* Space between inputs and button */
            align-items: stretch;
            /* Stretch inputs/buttons to full width */
        }

        /* Input Fields */
        input[type="email"],
        input[type="password"],
        input[type="text"] {
            padding: 12px 15px;
            border: 1px solid #ccc;
            border-radius: 8px;
            font-size: 1em;
            box-sizing: border-box;
            transition: border-color 0.3s;
        }

        input[type="text"] {
            /* Specific styling for the OTP field */
            letter-spacing: 2px;
            text-align: center;
            font-weight: bold;
        }

        input:focus {
            border-color: #008000;
            /* Green focus highlight */
            outline: none;
            box-shadow: 0 0 0 3px rgba(0, 128, 0, 0.1);
        }

        /* Buttons */
        button[type="submit"] {
            padding: 12px 20px;
            background-color: #008000;
            /* Primary green action button */
            color: #fff;
            border: none;
            border-radius: 8px;
            font-size: 1.1em;
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.1s ease;
        }

        button[type="submit"]:hover {
            background-color: #006400;
            /* Darker green on hover */
            transform: translateY(-1px);
        }

        /* Link Style (for otpfailed.ejs) */
        a {
            display: inline-block;
            margin-top: 20px;
            color: #C0516E;
            /* Pink accent color for links */
            text-decoration: none;
            font-weight: bold;
            padding: 10px 20px;
            border: 1px solid #C0516E;
            border-radius: 8px;
            transition: background-color 0.3s, color 0.3s;
        }

        a:hover {
            background-color: #C0516E;
            color: #fff;
        }
    </style>
</head>

<body>
    <div class="form-container">
        <h2>OTP Verification Failed</h2>
        <p>The OTP you entered is invalid or has expired. Please try again or resend the OTP.</p>
        <a href="/signin">Return to Sign In</a>
    </div>
</body>

</html>