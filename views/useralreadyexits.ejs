<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Warning: User Already Exists</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            text-align: center;
            padding: 50px;
            background-color: #e74c3c; /* Red background color */
            color: #fff; /* White text color */
        }

        h2 {
            font-size: 36px; /* Larger font size */
            margin-bottom: 20px;
        }

        #message {
            font-size: 24px; /* Medium font size */
            margin-bottom: 20px;
        }

        #countdown {
            font-size: 30px; /* Larger font size for the countdown */
            color: #fff; /* White text color */
        }

        p {
            font-size: 18px; /* Smaller font size for regular text */
        }

        a {
            color: #3498db;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <h2>Warning: User Already Exists</h2>
    <p id="message">A user with this mobile number already exists. Please try using a new mobile number.</p>

    <p id="redirectMessage">Redirecting in <span id="countdown">10</span> seconds...</p>

    <script>
        // Function to update countdown and redirect
        function redirectWithCountdown() {
            var countdownElement = document.getElementById('countdown');
            var countdownValue = parseInt(countdownElement.innerText);

            if (countdownValue > 1) {
                countdownElement.innerText = countdownValue - 1;
                setTimeout(redirectWithCountdown, 1000);
            } else {
                window.location.href = "/main";
            }
        }

        // Set initial timeout for redirection
        setTimeout(redirectWithCountdown, 1000);
    </script>

    <p>If you are not redirected, you can click <a href="/main">here</a>.</p>
</body>
</html>
